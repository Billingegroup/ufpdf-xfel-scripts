name: Deploy Documentation on Release

on:
  workflow_dispatch:

jobs:
  docs:
    uses: Billingegroup/release-scripts/.github/workflows/_publish-docs-on-release.yml@v0
    with:
      project: ufpdf_xfel_scripts
      c_extension: false
      headless: false
